// решение домашки

$(document).ready( function(){
        // Находим объекты
    $message = $('.form-control');
    console.log($message);

    //Создаем действие на действие мыши
    var mes = $message.val();
    console.log( 'mes')

});
